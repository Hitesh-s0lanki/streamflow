import { QueryDemo } from "./_components/QueryDemo";

export const metadata = {
  title: "TanStack Query Demo — StreamFlow",
  description:
    "Demo route showcasing TanStack Query: useQuery, useMutation, cache invalidation, and loading/error states.",
};

export default function DemoQueryPage() {
  return <QueryDemo />;
}
