# Test profile: minimal config for unit/integration tests
spring.application.name=streamflow-app-be-test

# Common overrides (optional; defaults in CommonConfig are fine)
streamflow.common.default-top-videos=5
streamflow.common.max-analytics-page=100
